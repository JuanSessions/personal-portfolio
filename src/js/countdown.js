const countdown = () => {

    let circleOuter = document.querySelector('.countdown-container');
    console.log("circleOuter:", circleOuter)

    //export default (() => {
    setTimeout(() => {
        // circleOuter.style.zIndex = '-1';
    }, 5000)



}

export default countdown;